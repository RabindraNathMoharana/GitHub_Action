name: context task
on: ['push']
jobs: 
  my-context:
    runs-on: ubuntu-latest
    steps:
      - name: "my step"
        run: echo "hello! $MY_ACTION"
        run: echo "hello mr! $MY_NAME"
        env: 
          MY_ACTION: ${{ github.action }}
          MY_NAME: ${{ github.actor }}
